# Cycle 1 continuation envelope C1-02

**Dated assignment record.** Its approved terms are pinned by the derivative
charter. Use the [cycle record](README.md#current-record) for the current
assignment and investigator; this document is not a live status page.

2026-09-08 · Version 0.2 · **APPROVED — assigned to Grok by the user.**
The user approved C1-02 on 2026-09-08 following the
[workload review](WORKLOAD_REVIEW.md). The approved proposal is version 0.1 at
root commit `a55b1021e4994ddb19f57a08188ec64a566bbdff`; this revision records
adoption without changing its scientific scope or resource limits. Grok may
prepare, freeze, and execute the in-scope protocol under the conditions below.
Record this adoption commit in the derivative charter. The original 100-start
envelope remains exhausted; preserve both old and new phase accounting.

## Objective and completion

Own a bounded **development continuation** of the existing utility-reset
question: determine how retaining or resetting the same acquired utilities
affects behavior across repeated encounters after the declared schema
migration, using fresh task cohorts. Complete the comparisons below or return
an accounted stop, with a reproducible report and a recommendation about the
value of a larger study. A positive reset effect is not a completion condition.

The assigned investigator is Grok; the user may reassign the agent/session. The
assignment covers `construct-memory-utility`; it includes no work in Cycle 2
and no automatic launch of a confirmatory study.

## Assigned scientific scope

Use the derivative's existing candidate schedule: **two separately acquired
histories, 12 tasks per history, and 396 planned episodes per history**.
For each history, three acquisition passes under each of the three existing
policies cost 108 episodes; three continuation passes under the eight existing
original/migrated branches cost 288. Total scheduled comparison: **792 episodes**.
The eight branches retain the four core retain/reset cells plus the similarity
and no-memory reference policies in both worlds.

Choose and commit the detailed sampling rule, seeds, execution order, and
analysis before comparison outcomes. Use two tasks in each of the six SQL
strata, with one assigned affected and one unaffected per stratum. Exclude the
six old development IDs and any new calibration tasks from the comparison.
Draw each cohort by the declared rule without selecting for actor performance.
Within-cohort tasks are distinct; separately drawn cohorts may overlap, which
must be reported. The investigator may inspect task metadata and reference
executability to define the population, with exclusions and reasons retained.

Retain the declared identifier-renaming migration and the information supplied
to the actor, including current schema. Clone the same acquired MemRL
checkpoint for the core cells; only utility retention/reset differs at the
fork. Preserve policy definitions, fixed-checkpoint retrieval replay, reward
accounting, and the original difference of differences. Report results by
history and continuation pass, including affected/unaffected tasks, retrieval
changes, and executed obsolete identifiers. Never modify the migration to
force stale SQL or condition primary outcomes on nonempty responses.

Two histories are **descriptive development**, not a population-effect or
precision claim. Repeated calls and tasks sharing memories are not independent
histories. Specify the aggregation across passes before running; retain
individual-pass results so immediate transfer and later adaptation are visible.
Do not pool these results with Pilot 1 or the channel smokes.

## Numbered resource caps

All limits are additional to historical spend and belong to phase `C1-02`.
Stop new work when any applicable cap is reached. These are maxima, not targets.

| Unit | Cap | Included work |
| --- | ---: | --- |
| Benchmark episode starts | **1,000** | 792 scheduled comparison episodes plus at most 208 calibration, diagnostic, or documented recovery starts; every failed start counts |
| Chat request attempts | **5,000** | Every actor, writer, calibration, diagnostic, and recovery request, including failures |
| Embedding request attempts | **8,000** | Every retrieval, write, update, calibration, and recovery request, including failures; also report batch sizes/input counts |
| Standalone chat probes | **32** | A sublimit within the 5,000 chat cap; these are never uncounted infrastructure work |

Use the existing local gpt-oss:20B and EmbeddingGemma 300M Q8_0 participant
families and pinned MemRL dependency. Record exact digests and observable
backend/template versions. Keep the current 2,048-token actor and 4,096-token
writer output caps. No paid endpoint, model upgrade, additional agent, or
shared-service reconfiguration is included. Use the derivative's isolated
database resources and coordinate through existing local exclusion mechanisms.

Before inference, implement durable reservations and guards for these limits,
including probes and embedding calls. The old episode-only guard is insufficient
for this envelope. Unknown/outstanding attempts remain spent; counters do not
reset at a new run directory. Preserve all request/response, token, timing,
state, and error evidence. Missing usage is unknown, not zero.

Simple extrapolation from the reviewed records puts the 792 scheduled episodes
at roughly **3.4–4.3 hours** and **4.8–5.5 million chat tokens**. Using all 1,000
episode starts gives roughly 4.3–5.4 hours and 6.0–7.0 million chat tokens before
standalone probes. These are planning estimates, not ceilings or performance
promises: longer memory histories, task complexity, and service contention can
increase costs. Embeddings and deterministic setup/verification must also be
reported. The enforceable limits are the counts above and the output caps.

## Decisions the derivative may make without another round-trip

The investigator may:

- Implement the schedule, budgets, provenance, independent outcome checks,
  artifact inventories, and an operational handoff.
- Select the detailed fresh-task sampling procedure, seeds, block order, and
  descriptive analysis within the scientific scope above; commit and freeze
  the protocol, then execute it. No separate approval to freeze or start that
  in-scope protocol is required.
- Spend the diagnostic allowance on concrete instrument questions. Keep its
  tasks separate and record the purpose before each batch. Do not spend the
  allowance searching for a preferred treatment effect or add comparison
  histories opportunistically.
- Make documented interface adaptations during calibration to realize the
  intended final-answer/action contract, under the fixed models and output
  caps. Preserve task information and supervision; never substitute reasoning
  for a missing final answer. Freeze one configuration across the comparison
  before its first outcome. Calibration evidence is not pooled with it.
- Repair software defects and perform offline reanalysis, preserving earlier
  code, scores, and raw evidence. During the frozen comparison, a repair must
  preserve the intended treatment and scoring meaning. A discovered scoring
  error needs an explicit correction record and sensitivity to affected claims.
- Design and test recovery that preserves completed cells and recorded state
  without replacing valid observed outputs. Suspend the affected run on an
  instrument failure, record the fault and recovery before further inference,
  and count all additional calls/starts. If the original conditions cannot be
  preserved, return the partial comparison; do not quietly start a replacement.

Ordinary task mistakes, nonempty but unparseable actions, and poor learned
memories remain participant outcomes. They are not reasons to keep repairing
the treatment until it succeeds. Freeze explicit instrument stopping/recovery
rules in the derivative protocol before model contact.

## Decisions that still return to the root/user

Return a concrete proposal before changing the research question, migration,
core contrast, number of comparison histories, evaluation population beyond
the assigned sampling scope, participant family, output caps, resource caps,
or a frozen treatment in a way that breaks comparability. A confirmatory study,
consumer-change experiment, or additional derivative requires a new assignment.

Progress reports are notifications when an in-scope stage completes, an
important surprise changes interpretation, or a decision is needed. They do
not themselves suspend already authorized independent work. Return once the
fixed comparison finishes or a cap/irrecoverable stop ends the assignment.
Report both histories, all attempts and costs, protocol deviations, adverse
cases, and what the findings change in the root account. Findings may justify
closing this line; they need not generate another engineering queue.
